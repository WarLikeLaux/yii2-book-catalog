# LLM Контекст проекта

> Этот файл — точка входа для LLM-агентов. Содержит ссылки на ключевые документы проекта.

## 🏗️ Архитектура

| Файл | Описание |
|------|----------|
| `/README.md` | Обзор проекта, структура, технологии |
| `/docs/ARCHITECTURE.md` | Детальное описание архитектуры проекта |

## 🔄 Воркфлоу (запускаются командами `#command`)

| Команда | Файл | Назначение |
|---------|------|------------|
| `#commit` | `/.agent/workflows/commit.md` | Генерация git-коммита с правильным форматом |
| `#changelog` | `/.agent/workflows/changelog.md` | Генерация записи в CHANGELOG.md |
| `#readme` | `/.agent/workflows/readme.md` | Обновление README.md |

## 🗄️ Автогенерируемая документация

Эти файлы генерируются автоматически и являются **источником истины** для схемы БД и структуры.

| Файл | Описание |
|------|----------|
| `/docs/auto/db.yaml` | Схема базы данных (таблицы, колонки, FK) |
| `/docs/auto/models.yaml` | ActiveRecord модели и их связи |
| `/docs/auto/routes.yaml` | HTTP роуты приложения |

## 🧪 Тестирование

| Ресурс | Описание |
|--------|----------|
| `/Makefile` | Все команды: `make dev`, `make test`, `make ci`, `make pr` |
| `/tests/unit/` | Unit-тесты |
| `/tests/functional/` | Функциональные тесты |
| `/tests/acceptance/` | Acceptance-тесты (Codeception) |

## 📁 Структура слоёв

```
domain/           → Сущности, Value Objects, Domain Events
application/      → UseCases, Commands, Ports (интерфейсы)
infrastructure/   → Репозитории, AR модели, внешние сервисы
presentation/     → Контроллеры, Forms, Validators, Views
```

## 🛠 Быстрые команды

```bash
make dev      # Автофикс + линт + анализ
make ci       # Линт + анализ + тесты
make pr       # Полная проверка перед PR
make test     # Все тесты
```
