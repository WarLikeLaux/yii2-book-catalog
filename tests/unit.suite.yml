# Конфигурация Codeception Test Suite
#
# Набор юнит (модульных) тестов.
# Тестирует классы в изоляции.

actor: UnitTester
modules:
    enabled:
        - Asserts
        - Yii2:
            part: [orm, email, fixtures]
            entryScript: index-test.php
