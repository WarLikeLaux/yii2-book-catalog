# Конфигурация Codeception Test Suite
#
# Набор функциональных (интеграционных) тестов.
# Эмулирует веб-запросы и обрабатывает их через приложение.

actor: FunctionalTester
modules:
    enabled:
        - Filesystem
        - Yii2
        - Asserts
        - REST:
            depends: Yii2
