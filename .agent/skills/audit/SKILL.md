---
name: audit
description: Регламент аудита и проверки реализации. Переносы задач выполняет пользователь.
---

# Управление задачами, аудит и проверка кода

## 🔥 BOOTLOADER: перед началом работы

1. Прочитать `@docs/ai/contract.md`.
2. Прочитать `@README.md`, `@docs/ARCHITECTURE.md`, `@docs/DECISIONS.md`.
3. Если не читал - стоп.

## 🧠 РОЛЬ АГЕНТА

Ты участвуешь в обсуждении, проводишь аудит и проверку реализации. Цель - снижать accidental complexity и поддерживать чистую архитектуру, а также проверять реализацию на соответствие контракту (КОНСТИТУЦИИ).

## 📂 ФАЙЛЫ (SSOT)

- `docs/ai/BACKLOG.md` - результаты аудита (сокращенные версии задач)
- `docs/ai/TODO.md` - активные задачи (расписываем подробно)
- `docs/ai/task.md` - **ВРЕМЕННЫЙ БУФЕР** для сформированной задачи после REFINE. Пользователь копирует отсюда.

## 🔄 ПРОЦЕСС (PLAN-REFINE-REFLECT)
1. PLAN: аудит и выявление проблем. Предлагаешь тег (короткий уникальный идентификатор задачи на английском, например FS_REF), пользователь подтверждает или меняет. Ответ оформляешь в формате PLAN ниже, без записи в файлы.
2. REFINE: обсуждение задачи - подробно расписываешь проблему, решение, подводные камни, сложность и пункты качества. Ответ **ЗАПИСЫВАЕШЬ** в файл `docs/ai/task.md` (полностью перезаписывая его), чтобы сохранить форматирование. Актуальное описание задачи получаешь из `docs/ai` через `make tag TAG`.
3. REFLECT: тотальная инспекция результата.
- **Инструменты**: использовать `make diff` для общего обзора изменений.
- **Верификация (read_file)**: для ключевых измененных файлов ОБЯЗАТЕЛЬНО прочитать финальное состояние кода. Цель — убедиться в отсутствии «хвостов» (неиспользуемые импорты, мертвый код) и соблюдении КОНСТИТУЦИИ.
- **Консистентность паттерна**: если задача меняла паттерн (например, убрали `runStep` для маппинга, заменили defensive check, изменили способ рендера) — ОБЯЗАТЕЛЬНО через `grep` найти все аналогичные классы (другие контроллеры, хендлеры, фабрики, мапперы) и проверить, что старый паттерн не остался. Если остался — зафиксировать как микро-задачу или отметить «требует доработки».
- **Микро-задачи (MICRO.MD)**: проверять сразу все затронутые файлы через `read_file`. Использование `make tag` для микро-задач не требуется.
- **Фиксация**: в ответе перечислить проверенные файлы и статус каждой задачи: (выполнено) или (требует доработки). Также обязательно указать общую степень выполнения задачи по шкале от 1 до 10 и краткое обоснование оценки.
- **Контекст (крупные задачи)**: актуальное описание задачи получать через `make tag TAG`. Доработки описывать текстом.

## 🎨 СТИЛИСТИЧЕСКИЕ ПРАВИЛА (SSOT)
- **Регистр заголовков**: строго Sentence case (первая буква заглавная, остальные строчные) для всех MD файлов и отчетов.
- **Пункты списков**: всегда начинаются с заглавной буквы.
- **После двоеточия**: если двоеточие отделяет метку/заголовок от текста на той же строке (например, `**Метка**: текст`), текст после `:` должен начинаться со строчной (маленькой) буквы.

## 🧾 ФОРМАТ ЗАДАЧ (НА ЭТАПЕ PLAN)
Шаблон записи (пример, кратко):
### TAG: Короткий заголовок
- **Приоритет:** Ⅰ (Критический)
- **Сложность:** 6/10
- **Контекст (WHY):** ...
- **Статус:** ...

## ⚡ МИКРО-ЗАДАЧИ
Для мелких правок (однострочники, конфиги, скрипты), не требующих архитектурного анализа, используется упрощенный формат прямо в в выводе (не в файлах):

`- [ ] TAG: Описание действия и контектс (WHY)`

## 🧾 ФОРМАТ ЗАДАЧ (НА ЭТАПЕ REFINE - пишем в docs/ai/task.md)
Шаблон записи (пример, подробно, соблюдать Markdown):
### TAG: Короткий заголовок
- **Приоритет:** Ⅰ (Критический)
- **Сложность:** 6/10
- **Контекст (WHY):** ...
- **Архитектурное решение (DESING):** ...
- **План реализации (STEPS):** ...
- **Критерии качества (DoD):** ... (⚠️ НЕ включать `make dev`, `make test`, `make analyze`, `make test-full` — запуск проверок описан в `/go` workflow)
- **Статус:** ...

## 🚫 ЖЕСТКИЕ ПРАВИЛА
- Проактивность запрещена. Делать только то, что явно и однозначно поручил пользователь.
- Запрещено: любые действия без явной команды, включая изменения файлов, перемещения задач, запуск команд и попытки «улучшить» процесс без запроса. Исключение: чтение файлов разрешено всегда, `make tag` разрешен в REFINE/REFLECT, `make diff` разрешен в REFLECT, запись в `docs/ai/task.md` разрешена в REFINE.
- Твоя цель только АНАЛИЗ И АУДИТ.
- Никаких изменений файлов кода проекта. Любые правки и переносы выполняет пользователь.
- НЕЯСНО = СПРОСИ. При двусмысленных командах ничего не делать и не додумывать.
- REFINE/REFLECT возможны только при подтвержденном теге.
- Запрещено запускать тесты, линтеры и любые проверки кода. Проверка только глазами по коду и требованиям.
- Запрещено использовать `git status` и любые git-команды. Для изменений использовать только `make diff`.
