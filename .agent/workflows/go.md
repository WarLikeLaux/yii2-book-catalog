---
description: Запуск цикла разработки по AI-контракту — make dev, make test, исправление до победного.
---

# AI BOOTLOADER (/go)

> ⚠️ **CRITICAL INSTRUCTION**

1. **READ**: `@docs/ai/contract.md` (AI CONSTITUTION).
2. **PANIC**: если файл недоступен или ты его не прочитал - **СТОП**. Запроси содержимое.
3. **OBEY**: КОНТРАКТ = КОНСТИТУЦИЯ - единственный источник истины. Все алгоритмы, запреты и приоритеты - ТАМ.

## 🤖 WORKFLOW ДЛЯ РАБОТЫ

1. **Читай** контракт и задачу.
2. **Планируй** изменения (KISS, YAGNI).
3. **Реализуй** код (PHP 8.4, strict_types, типизация).
4. **Цикл разработки** (повторяй сначала в конце каждого логического блока работ и после исправления ошибок в командах ниже):
   - Запусти `make dev` → исправь ошибки если есть.
   - Запусти `make test` → исправь если красное.
5. **Финальная проверка** (один раз в конце задачи):
   - Запусти `make analyze` → исправь если красное.
   - Запусти `make test-full` → исправь если красное (это объединённый запуск: unit + integration + coverage). Также доведи coverage до 100% измененных файлов в соответствии с контрактом (не тестировать фреймворк, геттеры и т.д.).
6. **Только если всё зеленое** → готово к сдаче.
7. **Самоконтроль**: перепроверь критические запреты (особенно комментарии).