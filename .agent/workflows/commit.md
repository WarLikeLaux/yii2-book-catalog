---
description: Генерация git коммита по #commit
---

# Workflow: Git Commit

Ты — технический лид, отвечающий за чистоту и профессионализм истории git. Анализируй изменения и генерируй ОДНО идеальное сообщение.

## Триггер
Команда `#commit` в сообщении пользователя.

## Шаги

### 0. Запустить проверки (make dev)
```bash
make dev
```
- **ОБЯЗАТЕЛЬНО** перед коммитом
- Если `make dev` падает — НЕ коммитить, сообщить пользователю об ошибках

### 1. Получить номер задачи
```bash
git log -1 --oneline
```
- Если пользователь указал номер (например, "для задачи 16"), используй его
- Если номер не указан, **ОБЯЗАТЕЛЬНО** посмотри последний коммит в `git log` и возьми номер оттуда

### 2. Проанализировать изменения
```bash
git diff --cached --stat   # Staged (приоритет)
git diff --stat            # Unstaged (если staged пуст)
```
- **Приоритет Staged**: Если в индексе (staged) есть изменения, анализируй **ТОЛЬКО** их
- **Unstaged**: Анализируй изменения вне индекса только если индекс пуст

### 3. Сгенерировать сообщение

**Формат:** `#ID - [сообщение]` (решетка, номер, пробел, тире, пробел)

**Пример (Правильно):**
```
#15 - fixed memory leak
```

**Пример (НЕПРАВИЛЬНО):**
```
 - fixed memory leak
#15-fixed memory leak
```

**Правила стиля:**
- **Язык:** Только технический английский
- **Регистр текста:** Используй строчные буквы (lowercase) для обычных слов
- **Регистр кода:** СТРОГО сохраняй регистр для имен классов, функций, переменных, файлов и констант (PascalCase, camelCase, SCREAMING_SNAKE_CASE)
- **Время:** Глаголы СТРОГО в прошедшем времени (**added**, **fixed**, **refactored**)
- **Пунктуация:** Без точек в конце. Без точек с запятой

### 4. Выполнить коммит
```bash
git commit -m "#ID - message"
```

## Словарь глаголов (используй строго из списка)

### ✨ New Features (Новое)
- **added** — добавление файла, функции или свойства
- **implemented** — реализация интерфейса или сложной логики
- **introduced** — внедрение новой зависимости или системы
- **created** — создание базовой структуры или компонента

### 🐛 Bug Fixes (Исправления)
- **fixed** — исправление ошибки, бага или тайпо
- **resolved** — решение конфликта или логической проблемы
- **patched** — критическое исправление "на скорую руку"
- **handled** — добавление обработки ошибок (try/catch)

### ♻️ Code Improvements (Улучшения)
- **refactored** — реструктуризация кода без изменения поведения
- **optimized** — улучшение производительности или памяти
- **simplified** — упрощение сложного куска кода
- **standardized** — приведение кода к единому стилю

### 📦 Updates & Maintenance (Обновления)
- **updated** — обновление данных, логики или версий
- **upgraded** — повышение версии зависимостей
- **synced** — синхронизация конфигов или типов
- **modified** — небольшое изменение существующего

### 🗑 Removal (Удаление)
- **removed** — удаление файлов или кода
- **deleted** — удаление ресурсов

### 🎨 UI/UX (Внешний вид)
- **styled** — изменение CSS/SCSS
- **aligned** — выравнивание элементов
- **tweaked** — мелкие визуальные правки

## Проактивность
Если пользователь явно просит "сгенерируй коммит" или "commit this", сразу предлагай готовую команду:
```bash
git add . && git commit -m "#ID - message"
```

## Строгие запреты
1. ⛔️ **NO** `git push` — КАТЕГОРИЧЕСКИ ЗАПРЕЩЕНО
2. ⛔️ **NO** Imperative mood (`Add`, `Fix`, `Update`) — Only Past Tense!
3. ⛔️ **NO** Multiple options — Only ONE best message
4. ⛔️ **NO** Generic messages (`fixed bugs`, `updated code`) — Be specific (`fixed layout bug`)
5. ⛔️ **NO** Explanations — Output ONLY the message or the git command if requested
