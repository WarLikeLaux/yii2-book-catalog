<div align="center">
<img src="docs/assets/hero-banner.png" alt="Yii2 Book Catalog: DDD, CQRS, Clean Architecture, CQS" width="800">

# Yii2 Book Catalog

**Enterprise-grade Clean Architecture on Yii2 Framework • PHP 8.4**

[![PHP Version](https://img.shields.io/badge/PHP-8.4-777BB4?style=for-the-badge&logo=php&logoColor=white)](https://www.php.net/)
[![Yii2](https://img.shields.io/badge/Yii2-Framework-blue?style=for-the-badge&logo=yii&logoColor=white)](https://www.yiiframework.com/)
[![MySQL](https://img.shields.io/badge/MySQL_/_PgSQL-Multi_DB-4479A1?style=for-the-badge&logo=mysql&logoColor=white)](https://www.mysql.com/)
[![Docker](https://img.shields.io/badge/Docker-Ready-2496ED?style=for-the-badge&logo=docker&logoColor=white)](https://www.docker.com/)
[![Tests](https://img.shields.io/badge/Tests-521_passed-success?style=for-the-badge&logo=codecov&logoColor=white)](#-тестирование-и-покрытие-кода)
[![Coverage](https://img.shields.io/badge/Coverage-100%25-brightgreen?style=for-the-badge&logo=codecov&logoColor=white)](#-тестирование-и-покрытие-кода)
[![Mutation Score](https://img.shields.io/badge/MSI-99%25-brightgreen?style=for-the-badge&logo=probot&logoColor=white)](#-тестирование-и-покрытие-кода)
[![AI Governance](https://img.shields.io/badge/AI-Governance-8A2BE2?style=for-the-badge&logo=googlegemini&logoColor=white)](docs/AI_WORKFLOW.md)

---

<p align="center">
  <b>🏛 Clean-ish Architecture</b> • <b>⚡ CQS Pattern</b> • <b>📨 Domain Events</b><br>
  <b>🎯 Value Objects</b> • <b>🔄 Async Fan-out</b> • <b>🤖 AI Governance</b>
</p>

</div>

---

👋 **Привет!** Рад видеть тебя в этом репозитории.

Этот проект - практическая реализация каталога книг на базе **Yii2 Basic** и **PHP 8.4**. Я использовал здесь **Clean-ish** архитектуру, чтобы показать, как можно писать масштабируемый код на классическом фреймворке.

Хотя принято считать, что AI-агенты лучше «дружат» с Python или Laravel, работа с терминальными агентами над этим проектом показала: классический **Yii2** всё ещё позволяет внедрять современные инженерные стандарты. Мы смогли построить здесь чистую архитектуру и доказать, что даже на «олдскульном» фреймворке можно работать качественно и актуально, используя нейросети как инструмент.

Главная цель - **отделить бизнес-логику от фреймворка**, добавить строгую типизацию и сделать асинхронные процессы надежными. Это прагматичный подход: мы не отказываемся от мощи **Yii2** в вебе и базе данных, но держим **Use Cases** и **порты** в полной изоляции. Так код получается чистым, понятным и на 100% покрытым тестами.

📋 Подробная история изменений доступна в [CHANGELOG.md](CHANGELOG.md).

🏗 **Подробное сравнение архитектуры** с типичным Yii2-подходом: [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md)

🤖 **Разработка с участием AI:** как устроено взаимодействие с агентами, контракты и автоматизация процессов: [docs/AI_WORKFLOW.md](docs/AI_WORKFLOW.md)

> **🤯 Зачем так сложно?**
>
> Этот проект - мой **Architectural Showcase**. Я хотел показать, что **Yii2** - это не только "быстрый старт" для новичков, но и отличный фундамент для серьезных систем. Главное - правильно его "приготовить": добавить **Clean Architecture**, **DDD** и **SOLID**.
>
> Здесь всё серьезно: **Deptrac** следит, чтобы слои не перемешивались, **Infection** проверяет качество тестов, а **PHPStan** на максималках (Level 9) ловит даже малейшие неточности.
>
> **🦖 Зачем взял этого "динозавра" (Yii2)?**
>
> Многие считают Yii2 устаревшим, но я показываю крутой симбиоз: проверенное временем ядро плюс возможности **PHP 8.4** (Enums, Readonly, Attributes).
> Я полностью убрал "магию" глобальных переменных: здесь всем управляет **Dependency Injection**. Код честный: если классу что-то нужно, он просит об этом в конструкторе.
>
> **🧠 Это просто генерация от AI?**
>
> Нет. Это не "нейрослоп" и не бездумная копипаста. Немаловажную часть кода написали автономные AI-агенты, работающие прямо в терминале под моим полным контролем. Я использую их как мощный "экзоскелет": они пишут код по моим строгим правилам и контрактам, а я проверяю и направляю каждое решение. Это пример того, как Senior + AI могут строить качественную архитектуру в разы быстрее, сохраняя полный контроль над качеством.
>
> **🤔 А есть подвох?**
>
> **Без него никак.** Я добавил сюда **здоровый прагматизм**, чтобы проект не стал "академически правильным, но бесполезным в жизни".
>
> Я не стал выкидывать `ActiveRecord` из инфраструктуры или писать свои велосипеды для БД. Где фреймворк реально экономит время (формы, роутинг, запросы к БД) - я его использую.
>
> **Но есть важное правило:** фреймворк заперт внутри инфраструктуры. Он инструмент, а не основа. Наша бизнес-логика вообще не знает о его существовании. Про эти "компромиссы" можно почитать в разделе [Прагматизм и компромиссы](#13-прагматизм-и-компромиссы).

---

## 📌 Навигация

- [✨ Ключевые особенности](#-ключевые-особенности)
- [🆚 Сравнение подходов](#-сравнение-подходов)
- [🚀 Установка и запуск](#-установка-и-запуск)
- [⚙️ Конфигурация](#️-конфигурация)
- [🛠 Технический стек](#-технический-стек)
- [🧪 Тестирование](#-тестирование-и-покрытие-кода)
- [📂 Структура проекта](#-структура-проекта)
- [🏗 Архитектурные решения](#-архитектурные-решения)
  - [1. Слой приложения (Use Cases, CQS, Ports)](#1-слой-приложения-use-cases-cqs-ports)
  - [2. Слой домена (Rich Domain Model)](#2-слой-домена-rich-domain-model)
  - [3. Слой представления (Yii2)](#3-слой-представления-yii2)
  - [4. Разделение ответственности: Use Cases vs сервисы представления](#4-разделение-ответственности-use-cases-vs-сервисы-представления)
  - [5. Инфраструктура и наблюдаемость](#5-инфраструктура-и-наблюдаемость)
  - [6. DTO & формы для валидации](#6-dto--forms-для-валидации)
  - [7. Инфраструктурный слой (Implementation)](#7-инфраструктурный-слой)
  - [8. Качество кода и стандарты](#8-качество-кода-и-стандарты)
  - [9. Гибридный поиск (universal search)](#9-гибридный-поиск-universal-search)
  - [10. Асинхронные операции (fan-out)](#10-асинхронные-операции-fan-out)
  - [11. Пагинация и кеширование](#11-пагинация-и-кеширование)
  - [12. Внедрение зависимостей](#12-внедрение-зависимостей)
  - [13. Прагматизм и компромиссы](#13-прагматизм-и-компромиссы)
  - [14. Наблюдаемость и трассировка](#14-наблюдаемость-и-трассировка)

---

## ✨ Ключевые особенности

| 🏛️ Архитектура | ⚡ Производительность |
| :--- | :--- |
| 🔹 **Clean-ish Architecture**<br>Компромисс между чистотой и прагматизмом | 🚀 **Async Fan-out**<br>Паттерн для масштабируемых уведомлений |
| 🔹 **Паттерн CQS**<br>Разделение команд и запросов | 🔍 **Гибридный поиск**<br>FullText + откат к LIKE |
| 🔹 **Value Objects**<br>`Isbn`, `BookYear` для бизнес-правил | 🛡 **Idempotency + Mutex**<br>Защита от дублей без гонок |
| 🔹 **Доменные события**<br>Асинхронное взаимодействие | ⚡ **PJAX**<br>Мгновенная фильтрация |
| **🧪 Качество кода** | **🐳 DevOps Ready** |
| ✅ **521 тестов** (1194 assertions)<br>100% покрытие кода тестами | 🐳 **Docker Compose**<br>Полный стек одной командой |
| ✅ **PHPStan Level 9**<br>Кастомные правила (Custom Rules) | 🛠 **Makefile**<br>Автоматизация рутины |
| ✅ **Мутационное тестирование**<br>Infection PHP (MSI 99%) | 📚 **Генерация документации**<br>Yii2 API + OpenAPI |
| ✅ **Авто-рефакторинг**<br>Rector | 🏗 **Контроль архитектуры**<br>Deptrac |

[↑ К навигации](#-навигация)

### 🆚 Сравнение подходов

| Характеристика | Стандартный Yii2 (MVC) | Чистая Архитектура |
| :--- | :--- | :--- |
| **Бизнес-логика** | Размыта по контроллерам / Active Record | Изолирована в **Use Cases** |
| **Доступ к сервисам** | Service Locator (`Yii::$app->...`) | Strict **Dependency Injection** |
| **ActiveRecord** | "God Object" (Логика + SQL + Валидация) | Только для сохранения (в Репозиториях) |
| **Валидация** | `$model->rules()` на все случаи жизни | **Формы** (input) + **Value Objects** (business) |
| **События** | Магические `on(EVENT)` и поведения | Явные **Domain Events** + Dispatcher |
| **Тестирование** | Сложно (Global State, БД) | Легко (Unit, Mocking, InMemory) |

[↑ К навигации](#-навигация)

## 🚀 Установка и запуск

### ⚡ Быстрый старт (3 команды)

```bash
# 1. Клонируем проект
git clone https://github.com/WarLikeLaux/yii2-book-catalog.git
cd yii2-book-catalog

# 2. Установка (интерактивно)
make install
# или принудительно (без вопросов):
make install-force

# 3. Готово! 🎉
open http://localhost:8000
```


> 💡 **Что делает `make install`:**
> - 🐳 Поднимает Docker контейнеры (PHP 8.4 + MySQL 8 / PgSQL + воркер очереди)
> - 📦 Устанавливает Composer зависимости
> - 🗄 Применяет миграции БД
> - 🌱 Наполняет базу демо-данными

<details>
<summary><b>🔑 Тестовые учётные данные</b></summary>

| Логин | Пароль |
|-------|--------|
| `admin` | `admin` |
| `demo` | `demo` |

</details>

Приложение будет доступно по адресу: **[http://localhost:8000](http://localhost:8000)**

### 🛠 Команды

Весь проект управляется через `Makefile`. Запустите `make help`, чтобы увидеть полный список команд (более 30 утилит).

**Самые важные:**

```bash
make install    # полная установка и запуск 🚀
make test       # запуск основных тестов (Unit + Integration) 🧪
make dev        # авто-фикс стиля и статический анализ 🛡️
make up         # управление контейнерами (up/down) 🐳
```

[↑ К навигации](#-навигация)

## ⚙️ Конфигурация
Все настройки приложения вынесены в файл `.env`. При установке (`make install`) он создается автоматически из `.env.example`.

### 🌍 Основные настройки

| Переменная | Значение по умолчанию | Описание |
| :--- | :--- | :--- |
| `YII_ENV` | `dev` | Окружение: `dev`, `prod`, `test` |
| `YII_DEBUG` | `true` | Включить режим отладки Yii2 |
| `SMS_API_KEY` | `MOCK_KEY` | Ключ для SMS шлюза. `MOCK_KEY` пишет SMS в лог. |

### 🗄️ База данных

В проекте реализована **горячая смена драйвера БД**.

| Переменная | Значение | Описание |
| :--- | :--- | :--- |
| `DB_DRIVER` | `mysql` / `pgsql` | Выбор активной базы данных |
| `DB_NAME` | `yii2basic` | Имя базы данных |
| `DB_USER` | `yii2` | Пользователь БД |
| `DB_PASSWORD` | `secret` | Пароль пользователя |
| `MYSQL_PUBLIC_PORT` | `33060` | Внешний порт MySQL (доступ с хоста) |
| `PGSQL_PUBLIC_PORT` | `54320` | Внешний порт PostgreSQL (доступ с хоста) |

### 🔍 Инфраструктура и отладка

Для локальной разработки используется полный стек [Buggregator](https://buggregator.dev).

| Переменная | Порт | Сервис |
| :--- | :--- | :--- |
| `APP_PORT` | `8000` | Основной веб-сервер приложения |
| `SWAGGER_PORT` | `8081` | Swagger UI документация |
| `BUGGREGATOR_UI_PORT` | `8090` | Дашборд отладки (Logs, Dumps, Mail, Traces) |
| `REDIS_PUBLIC_PORT` | `6379` | Внешний доступ к Redis |

> 💡 **Совет:** Если порты заняты, просто измените их в `.env` и перезапустите контейнеры через `make up`.

### 🎛️ Интерактивная настройка

Если вы не хотите править файлы вручную, запустите мастер настройки:

```bash
make env   # или make configure
```

Скрипт пошагово спросит все параметры (порты, пароли, драйвер БД) и обновит `.env`. Это полезно, если на вашем компьютере уже занят порт 8000 или 3306.

[↑ К навигации](#-навигация)

## 🛠 Технический стек

| Категория | Технология | Описание |
|-----------|------------|----------|
| **Язык** | [![PHP](https://img.shields.io/badge/PHP-8.4-777BB4?logo=php&logoColor=white)](https://www.php.net/) | PHPStan Level 9, Strict Types, Constructor Promotion |
| **Фреймворк** | [![Yii2](https://img.shields.io/badge/Yii-2.0-blue?logo=yii)](https://www.yiiframework.com/) | Basic Template с DI Container |
| **База(-ы) данных** | [![MySQL](https://img.shields.io/badge/MySQL_/_PgSQL-4479A1?logo=mysql&logoColor=white)](https://www.mysql.com/) | InnoDB / PostgreSQL 16 + FullText Search |
| **Очереди** | `yii2-queue` | DB Driver + Fan-out Pattern |
| **Тестирование** | [![Codeception](https://img.shields.io/badge/Codeception-5.0-purple)](https://codeception.com/) | Unit + Integration + E2E, 100% Coverage |
| **Инфраструктура** | [![Docker](https://img.shields.io/badge/Docker-Compose-2496ED?logo=docker&logoColor=white)](https://www.docker.com/) | PHP 8.4 + MySQL 8 / PgSQL 16 + Redis + очередь |
| **Качество** | `Rector`, `PHPStan`, `Deptrac`, `Advisories` | Strict Static Analysis & Security Checks |

[↑ К навигации](#-навигация)

### 🧪 Тестирование и покрытие кода

Проект демонстрирует, как **Clean Architecture** превращает тестирование из "рутины" в мощный инструмент быстрой разработки.

#### 🎯 Почему тестов так много и они такие быстрые?
*   **Decoupling:** бизнес-логика (Use Cases) полностью отделена от фреймворка и БД. Мы тестируем "чистый PHP", что позволяет выполнять сотни Unit-тестов за миллисекунды.
*   **Strict DI:** все зависимости внедряются через конструктор. Это позволяет легко подменять тяжелые части (БД, очереди, SMS-шлюзы) на сверхбыстрые **Mocks** или **In-memory** реализации.
*   **No Global State:** отсутствие обращений к `Yii::$app` в доменном слое делает тесты изолированными и стабильными.

<table>
<tr>
<td align="center"><b>521</b><br>Tests</td>
<td align="center"><b>1194</b><br>Assertions</td>
<td align="center"><b>100%</b><br>Coverage</td>
<td align="center"><b>~12.7s</b><br>Runtime</td>
</tr>
</table>

Отдельный прогон E2E: 17 сценариев, 41 проверка (`make test-e2e`).

#### 🏗 Пирамида тестирования
1.  **Unit Tests (449):** тестируют доменные сущности, Value Objects и Use Cases в полной изоляции. Это фундамент стабильности.
    ```bash
    make test-unit
    ```
2.  **Integration Tests (72):** проверяют взаимодействие слоев: работу репозиториев с реальной БД (MySQL/PgSQL), маппинг событий в очереди и интеграцию с Yii2 компонентами.
    ```bash
    make test-integration
    ```
3.  **E2E Tests (17):** имитируют действия пользователя в браузере. Проверка критических путей: создание книги, публикация, подписка (прогон выполняется отдельно).
    ```bash
    make test-e2e
    ```

#### 🦠 Мутационное тестирование (Infection)
Мы идем дальше простого покрытия строк (Line Coverage). Проект проходит **Infection Testing** - это "тестирование тестов".
*   Infection вносит ошибки в исходный код (мутанты) и проверяет, "заметят" ли это тесты.
*   **Mutation Code Coverage 100%** и **MSI (Mutation Score Indicator) 99%** означают, что большая часть изменений логики не пройдет незамеченной.
```bash
make test-infection
```

[↑ К навигации](#-навигация)

## 📂 Структура проекта

```text
yii2-book-catalog/
├── assets/                  # Frontend assets
├── bin/                     # Кастомные скрипты
├── application/             # Слой приложения (Use Cases, Queries, Ports)
│   ├── books/               # Модуль "Книги"
│   │   ├── commands/        # DTO команд (CreateBookCommand)
│   │   ├── queries/         # DTO запросов (BookReadDto)
│   │   └── usecases/        # Сценарии (CreateBookUseCase)
│   ├── authors/             # Модуль "Авторы" (аналогичная структура)
│   ├── subscriptions/       # Модуль "Подписки"
│   ├── reports/             # Модуль "Отчеты"
│   ├── common/              # Общие компоненты (IdempotencyService, DTO)
│   └── ports/               # Интерфейсы (Events, Mutex, Repositories)
├── domain/                  # Слой домена (чистый PHP)
│   ├── entities/            # Rich Entities (Book, Author)
│   ├── events/              # Domain Events & QueueableEvent
│   ├── exceptions/          # Domain Exceptions (StaleDataException)
│   ├── services/            # Domain Services (BookPublicationPolicy)
│   ├── specifications/      # Specifications (поиск/фильтрация)
│   └── values/              # Value Objects (Isbn, BookYear)
├── infrastructure/          # Инфраструктурный слой (реализации портов)
│   ├── adapters/            # Адаптеры (Events, Mutex, Transactions)
│   │   └── decorators/      # Tracing Decorators для адаптеров
│   ├── listeners/           # Event Listeners (ReportCacheInvalidation)
│   ├── persistence/         # ActiveRecord модели (только для маппинга)
│   ├── phpstan/             # Custom PHPStan rules
│   ├── queue/               # Задачи очереди
│   ├── repositories/        # Реализации репозиториев (Strict DI)
│   │   └── decorators/      # Tracing Decorators
│   └── services/            # Инфраструктурные сервисы (Logger, Storage)
├── presentation/            # Слой представления (Yii2 & web)
│   ├── auth/                # Аутентификация
│   ├── controllers/         # Тонкие контроллеры
│   ├── books/               # Модуль "Книги" (формы, handlers, mappers)
│   ├── authors/             # Модуль Авторы
│   ├── components/          # Базовые UI-компоненты
│   ├── common/              # Общие компоненты слоя представления
│   │   ├── adapters/        # Адаптеры (PagedResultDataProvider)
│   │   ├── filters/         # Фильтры (IdempotencyFilter)
│   │   ├── handlers/        # Общие handlers
│   │   └── services/        # Сервисы (WebUseCaseRunner)
│   ├── dto/                 # DTO для представления
│   ├── mail/                # Шаблоны писем
│   ├── reports/             # Модуль "Отчеты"
│   ├── subscriptions/       # Модуль "Подписки"
│   ├── views/               # Шаблоны (представления)
│   └── widgets/             # UI-виджеты
├── commands/                # Console контроллеры (CLI)
├── config/                  # Конфигурация приложения
├── db-data/                 # Данные локальной БД (volume)
├── docker/                  # Docker-конфигурация
├── messages/                # Переводы i18n
├── migrations/              # Миграции БД
├── runtime/                 # Runtime кэш/логи
├── tests/                   # Тесты
├── tools/                   # Инструменты разработки (PHPUnit, Rector)
├── web/                     # Web root
└── docs/                    # Документация
```

**Примечание:** В коде используется namespace `app\`, что соответствует стандартному Yii2 алиасу `@app`. Структура директорий в корне проекта соответствует namespace-ам (например, `application/` → `app\application\*`).

[↑ К навигации](#-навигация)

## 🏗 Архитектурные решения

### 1. Слой приложения (Use Cases, CQS, Ports)
Я разделил чтение и запись (паттерн **CQS**) и вынес все внешние зависимости в порты:
*   **Запись (команды):** любое изменение в системе (создание книги, подписка) - это отдельный **Use Case**. Данные в него попадают через строго типизированные **Command DTO**. Это гарантирует, что мы не передаем "кашу" из параметров.
*   **Чтение (запросы):** чтобы не тянуть тяжелые модели там, где нужно просто показать список, я использую Query-порты. Они возвращают легкие DTO, что экономит память и время.
*   **Порты:** это просто интерфейсы. Use Cases зависят от них, а не от конкретной базы или почтового сервиса. Это позволяет менять "начинку" (например, MySQL на PostgreSQL), не трогая логику.

[↑ К навигации](#-навигация)

### 2. Слой домена (Rich Domain Model)
Здесь живет сама суть приложения, без привязки к вебу или базе данных:
*   **Rich Entities:** сущность `Book` - это не просто набор полей. Она сама умеет себя публиковать или менять статус, соблюдая бизнес-правила.
*   **Контроль изменяемости (immutability):** доменные сущности используют `private(set)` и меняются только через методы с проверками инвариантов.
*   **Value Objects:** такие штуки как `Isbn` или `BookYear` - это маленькие объекты, которые гарантируют свою правильность. Нельзя создать "невалидный ISBN", он упадет с ошибкой еще при попытке создания.
*   **Domain Events:** используются для связи между частями системы. Например, когда книга опубликована (`BookPublishedEvent`), система уведомляет подписчиков. При этом сама логика публикации не знает, как именно пойдет уведомление - через SMS, Email или очередь. Это делает код гибким.

[↑ К навигации](#-навигация)

### 3. Слой представления (Yii2)
Этот слой отвечает за всё, что видит пользователь и как он взаимодействует с системой:
*   **Контроллеры:** максимально тонкие. Их работа - просто принять HTTP-запрос, запустить валидацию формы и вызвать нужный Use Case.
*   **Формы:** мы не используем ActiveRecord напрямую в формах. Для каждой задачи (создание книги, фильтрация) есть своя форма, которая проверяет данные.
*   **Handlers & Factories:** разделяют логику отображения. Хендлеры обрабатывают действия (команды), а фабрики подготавливают данные для вьюх (списки, DTO).
*   **Модели представления (View Models):** чтение отделено от отображения через `BookViewModel`, чтобы не тянуть доменные объекты в UI.
*   **Фильтры запросов:** идемпотентность и ограничение частоты (rate limiting) оформлены отдельными фильтрами.
*   **WebUseCaseRunner:** это "умный" запускальщик для Use Cases. Он сам ловит ошибки, пишет их в лог и выводит красивые уведомления пользователю. Нам не нужно писать `try-catch` в каждом контроллере.

[↑ К навигации](#-навигация)

### 4. Разделение ответственности: Use Cases vs сервисы представления

**Use Cases (слой приложения)** - бизнес-логика:
*   Работают с готовыми Command/DTO объектами (уже валидные данные)
*   Не знают о формах, HTTP, валидации форм, форматах ответов
*   Независимы от способа представления (HTTP, CLI, API, тесты)
*   Содержат чистую бизнес-логику: транзакции, бизнес-правила, координация репозиториев

**Слой представления** - разделен на Handlers и Factories:
*   **Command Handlers:** принимают заполненные формы, маппят их в команды и выполняют Use Cases. Не зависят от `Request` или `Response`.
*   **Фабрики данных представления (View Data Factories):** подготавливают данные для отображения (списки авторов, книги).
*   **Контроллер:** выступает как Orchestrator. Загружает данные из HTTP-запроса, запускает валидацию форм, вызывает хендлеры и формирует ответ.

**Пример разделения:**

```php
// BookController.php
public function actionCreate(): string|Response|array
{
    $form = new BookForm();

    if ($this->request->isPost && $form->loadFromRequest($this->request)) {
        if ($this->request->isAjax) {
            $this->response->format = Response::FORMAT_JSON;
            return ActiveForm::validate($form);
        }

        if ($form->validate()) {
            $bookId = $this->commandHandler->createBook($form);
            if ($bookId !== null) {
                return $this->redirect(['view', 'id' => $bookId]);
            }
        }
    }

    return $this->render('create', [
        'model' => $form,
        'authors' => $this->viewDataFactory->getAuthorsList(),
    ]);
}

// BookCommandHandler.php
final readonly class BookCommandHandler
{
    public function createBook(BookForm $form): ?int
    {
        $tempFile = $this->uploadCover($form);
        $permanentRef = $tempFile instanceof TemporaryFile ? $this->fileStorage->moveToPermanent($tempFile) : null;
        $command = $this->mapper->toCreateCommand($form, $permanentRef);

        return $this->useCaseRunner->executeWithFormErrors(
            fn(): int => $this->createBookUseCase->execute($command),
            Yii::t('app', 'book.success.created'),
            function (DomainException $e) use ($form, $permanentRef): void {
                if ($permanentRef instanceof StoredFileReference) {
                    $this->fileStorage->delete((string)$permanentRef);
                }
                $this->addFormError($form, $e);
            }
        );
    }
}
```

[↑ К навигации](#-навигация)

### 5. Инфраструктура и наблюдаемость
Здесь - всё, что касается внешнего мира и "железа":
*   **Две базы данных:** проект умеет работать и с MySQL 8.0, и с PostgreSQL 16. Можно переключиться одной строчкой в `.env`.
*   **Трассировка (Distributed Tracing):** мы видим всё, что происходит "под капотом". Каждый запрос в базу или задачу в очереди можно посмотреть в Buggregator: сколько времени это заняло, какие были параметры и где возникла задержка.
*   **Интерактивный Shell:** через `make shell` можно зайти прямо в работающее приложение и потыкать его изнутри. Очень удобно для отладки.
*   **PSR-20 Clock:** время инкапсулировано через `ClockInterface`, что упрощает тестирование и контроль времени.

[↑ К навигации](#-навигация)

### 6. DTO & формы для валидации
Слой представления отделен от домена.
*   **Формы (`presentation/{feature}/forms`):** валидируют сырые пользовательские данные (HTTP request).
*   **Command DTO (`application/{feature}/commands`):** передают валидные данные в Use Case.
*   **PaginationDto (`application/common/dto`):** чистый DTO для пагинации.
*   Это позволяет разлепить валидацию HTTP-запроса и бизнес-правила (которые живут в Value Objects).

[↑ К навигации](#-навигация)

### 7. Инфраструктурный слой
*   **ActiveRecord и DB:** реализации портов живут в `infrastructure` (namespace: `app\infrastructure`).
*   **Очередь/хранилище файлов:** подключаются через интерфейсы и DI.
*   **Event Publisher Adapter:** `YiiEventPublisherAdapter` диспатчит события синхронным слушателям и отправляет в очередь только `QueueableEvent`. Маппинг событий в job делает `EventToJobMapper`. Это позволяет Use Cases оставаться независимыми от фреймворка.
*   **Optimistic Locking:** репозитории используют `version` и выбрасывают `StaleDataException` при устаревших данных.
*   **Пагинация:** query-сервисы используют `ActiveDataProvider` для выполнения запросов (сохранение eager loading через `with()`), но возвращают чистый `PaginationDto` вместо передачи framework-объекта в application layer.

[↑ К навигации](#-навигация)

### 8. Качество кода и стандарты
Чтобы проект не превратился в свалку, за качеством следят роботы:
*   **Строгая типизация:** весь код написан с `strict_types=1`. Никаких угадываний типов.
*   **PHPStan (Level 9):** самый жесткий уровень проверки. Если PHPStan недоволен - код не пройдет проверку в CI.
*   **Rector:** автоматически обновляет наш код до PHP 8.4 и исправляет старые подходы на современные.
*   **Линдер:** команда `make dev` сама причешет код под единый стандарт, чтобы всё выглядело красиво.

[↑ К навигации](#-навигация)

### 9. Гибридный поиск (universal search)
Реализован "умный" поиск по каталогу без использования внешних движков (Elasticsearch), но с оптимизацией под MySQL и чистыми спецификациями в домене.
*   **FullText Index:** используется для поиска по `title`, `description` и `authors.fio`.
*   **LIKE fallback:** поиск по `title`, `description` и авторам работает даже без FullText (устойчивость в тестах/локально).
*   **Prefix/Exact Match:** для ISBN используется префикс, для года - точное совпадение.
*   **Specifications:** `BookSearchSpecificationFactory` собирает критерии, а query-порт выполняет `searchBySpecification()`.
*   **UX:** обернуто в **PJAX** для фильтрации без перезагрузки страницы.

[↑ К навигации](#-навигация)

### 10. Асинхронные операции (fan-out)
Чтобы тяжелые задачи (например, рассылка SMS тысячам людей) не тормозили сайт:
1.  **Событие:** Use Case просто говорит: "Книга вышла!". Но он не знает, кому и как отправлять.
2.  **Очередь:** инфраструктура ловит это событие и кладет его в очередь. 
3.  **Fan-out:** сначала создается одна "мастер-задача", которая находит всех подписчиков. А потом для каждого подписчика создается своя маленькая подзадача.
*   **Результат:** если одно SMS не отправится - остальные всё равно уйдут. И мы можем обрабатывать тысячи уведомлений параллельно на разных воркерах.

[↑ К навигации](#-навигация)

### 11. Пагинация и кеширование
Чтобы приложение работало быстро на больших объемах данных:
*   **Проблема:** обычная пагинация Yii2 привязывает логику к фреймворку.
*   **Решение:** мы создали свои DTO для страниц. Репозиторий делает выборку и возвращает "чистый" результат.
*   **Адаптер:** только в самом конце, перед выводом на экран, специальный адаптер превращает наши DTO обратно в формат, понятный виджетам Yii2.
*   **Результат:** логика не знает о существовании пагинации фреймворка, но пользователю всё равно удобно листать страницы.

[↑ К навигации](#-навигация)

### 12. Внедрение зависимостей
В проекте нет вызовов вроде `Yii::$app->db`. Все зависимости передаются явно:
*   **Порты:** это "заказы" нашего приложения. Например: "мне нужно уметь отправлять SMS".
*   **Реализации:** в инфраструктуре мы пишем: "вот как именно мы отправляем SMS через конкретный шлюз".
*   **Контейнер:** весь проект связан через DI-контейнер. Если классу нужен репозиторий или сервис - он просто пишет об этом в конструкторе. Это делает код максимально понятным: ты сразу видишь, от чего зависит класс.
*   **DI в фоновых задачах:** стандартный `yii2-queue` сериализует объекты задач, что делает невозможным внедрение зависимостей через конструктор. Чтобы избежать использования глобального `Yii::$container` (Service Locator) внутри Job-ов, мы используем паттерн **HandlerAwareQueue** + **JobHandlerRegistry**. Это сохраняет прозрачность зависимостей даже в асинхронной среде.

[↑ К навигации](#-навигация)

### 13. Прагматизм и компромиссы
Мы не стремились к "идеальной архитектуре из учебника", потому что она часто мешает работать. Мы выбрали **Clean-ish** подход.

#### 13.1. ActiveRecord в репозиториях
**Компромисс:** мы используем всю мощь `ActiveRecord` для запросов к базе.
**Почему:** зачем писать SQL руками или городить сложные Query Builders, если Yii уже отлично это делает? Мы используем нативные связи (`with`), чтобы не было проблем N+1, но на выходе из репозитория всегда отдаем чистые объекты (DTO), а не модели фреймворка.

#### 13.2. Компоненты Yii2 в вебе
**Компромисс:** мы оставили `ActiveForm` и стандартные ответы Yii2.
**Почему:** эти штуки экономят уйму времени при верстке и обработке форм. Главное, что наш доменный слой про них ничего не знает.

#### 13.3. Валидация без фанатизма
**Компромисс:** мы не плодим классы-валидаторы на каждый чих.
**Почему:** если проверка простая (формат почты, обязательное поле) - ее делает форма. Если проверка сложная (правила бизнеса) - ее делает сам домен или репозиторий. Например, проверка на уникальность ISBN делается прямо в базе, потому что это единственный способ гарантировать отсутствие дублей на 100%.

#### 13.4. Упрощенная авторизация (Showcase mode)
**Компромисс:** учетные данные администратора (`admin`/`admin`) временно оставлены в коде, а пароли проверяются без хеширования.
**Почему:** это осознанное решение для демонстрационного проекта. Мы сфокусировались на архитектуре бизнес-логики (Books, Authors), а не на инфраструктуре RBAC/Identity, которая в Yii2 часто требует либо тяжелых модулей, либо много бойлерплейта. В реальном проекте это было бы первой задачей на исправление, но здесь это позволяет запустить проект "из коробки" без настройки БД пользователей.

Это и есть **прагматизм**: фреймворк помогает нам там, где он силен, но не мешает развивать и тестировать основную логику.

[↑ К навигации](#-навигация)

### 14. Наблюдаемость и трассировка
Мы внедрили систему трассировки, чтобы не гадать, почему запрос тормозит или что именно пошло не так в очереди.

#### 🏗 Как это работает?
Вам не нужно вручную прокидывать логи по всему коду. Мы используем **декораторы**: они оборачивают ваши репозитории и сервисы, автоматически замеряя время и записывая детали (например, SQL-запросы).

#### 🚀 Что вы увидите в Buggregator?
*   **Timeline:** полная лента событий - от захода пользователя на страницу до последнего запроса в БД.
*   **SQL-запросы:** прямо в трейсе видно, какой SQL выполнился, сколько миллисекунд он занял и какие данные вернул.
*   **Очереди:** можно проследить путь задачи от момента создания до выполнения в воркере.

Это позволяет найти "бутылочное горлышко" в коде за считанные секунды, просто взглянув на график в панели отладки.

[↑ К навигации](#-навигация)

---

<div align="center">

### 📊 Статистика проекта

![Source Code](https://img.shields.io/badge/Source_Code-7.4k+-blue?style=for-the-badge&logo=icloud&logoColor=white)
![Test Code](https://img.shields.io/badge/Test_Code-10.1k+-blue?style=for-the-badge&logo=codecov&logoColor=white)
![Source Files](https://img.shields.io/badge/Source_Files-214-purple?style=for-the-badge&logo=php&logoColor=white)
![Test Files](https://img.shields.io/badge/Test_Files-125-orange?style=for-the-badge&logo=codecov&logoColor=white)
![Test Coverage](https://img.shields.io/badge/Coverage-100%25-brightgreen?style=for-the-badge&logo=codecov&logoColor=white)
![PHPStan](https://img.shields.io/badge/PHPStan-Level_9_+_Strict-brightgreen?style=for-the-badge&logo=probot&logoColor=white)

<br>

**Made with ❤️ using [Yii2 Framework](https://www.yiiframework.com/)**

*Clean-ish Architecture • DDD • CQRS • Event-Driven*

</div>
