grumphp:
  hooks_dir: ~
  hooks_preset: local
  git_hook_variables:
    VAGRANT_HOST_DIR: .
    VAGRANT_PROJECT_DIR: /app
    EXEC_GRUMPHP_COMMAND: docker compose exec -T php
  tasks:
    phpcs:
      standard: [phpcs.xml.dist]
    jsonlint: ~
    xmllint: ~
    shell:
      scripts:
        deptrac:
          command: "make _arch-deptrac"
          description: "🏛️  Checking architecture layers (Deptrac)"
        phparkitect:
          command: "make _arch-arkitect"
          description: "🏛️  Checking architecture patterns (PHPArkitect)"
  testsuites: []
  extensions: []
