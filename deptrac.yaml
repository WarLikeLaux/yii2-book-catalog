deptrac:
  paths:
    - application
    - domain
    - infrastructure
    - presentation
  
  exclude_files:
    - '#.*test.*#'
    - '#.*bootstrap.*#'

  layers:
    - name: Domain
      collectors:
        - type: directory
          value: domain/.*
    
    - name: Application
      collectors:
        - type: directory
          value: application/.*
    
    - name: Infrastructure
      collectors:
        - type: directory
          value: infrastructure/.*
    
    - name: Presentation
      collectors:
        - type: directory
          value: presentation/.*
    
    - name: Framework
      collectors:
        - type: classLike
          value: ^(Yii|yii\\).*  

    - name: Vendor
      collectors:
        - type: classLike
          value: ^(Ramsey\\|Psr\\|Symfony\\|Doctrine\\).*  

  ruleset:
    Domain:
      - Vendor
    
    Application:
      - Domain
      - Vendor
    
    Infrastructure:
      - Domain
      - Application
      - Vendor
      - Framework
    
    Presentation:
      - Domain
      - Application
      - Vendor
      - Framework

  skip_violations:
    app\presentation\subscriptions\forms\SubscriptionForm:
      - app\infrastructure\persistence\Author
    app\presentation\auth\forms\LoginForm:
      - app\infrastructure\persistence\User