# AI CONSTITUTION — ВЫСШИЙ ЗАКОН ПРОЕКТА

> Это твоя **КОНСТИТУЦИЯ**. Любое нарушение — дефект.
> Приоритет: **KISS → YAGNI → DRY → SOLID**  
> Язык общения: **СТРОГО РУССКИЙ**

---

## 🚫 КРИТИЧЕСКИЕ ЗАПРЕТЫ

| Запрет | Почему |
|--------|--------|
| **ENGLISH ANSWERS** | **КАТЕГОРИЧЕСКИ ЗАПРЕЩЕНО**. Ты обязан отвечать и комментировать ТОЛЬКО на русском. Никаких "Okay", "Done". |
| **Правила комментариев** | Перед добавлением комментариев и PHPDoc обязательно прочитать раздел «ПРАВИЛА КОММЕНТАРИЕВ» и следовать ему. |
| **FQCN в коде** | `\App\Service\UserService` → используй `use` + короткое имя |
| **Нет `declare(strict_types=1)`** | Обязательно в каждом PHP файле |
| **PHPStan errors** | Level 9 обязателен. 0 ошибок в `make analyze` |
| **Вложенность > 2** | Guard clauses + декомпозиция |
| **Функция > 50 строк** | Декомпозиция на хелперы |
| **> 4 параметров** | Группируй в DTO/Command |
| **Boolean флаги** | Отдельные методы вместо `$flag = true` |
| **Service Locator** | Запрещено использовать `Yii::$app->get(...)` или глобальный стейт. Только DI через конструктор. |
| **Vendor Lock-in** | Код должен работать и на MySQL, и на PostgreSQL. Запрещены специфичные функции без адаптеров/драйверов. |
| **English answers** | Агент обязан отвечать и комментировать только на русском языке. |
| **`git commit`** | **NUCLEAR PROHIBITION**. ЗАПРЕЩЕНО делать коммиты проактивно. СТРОГО 1 коммит на 1 триггер `#commit`. Любой автоматический или самостоятельный коммит — КРИТИЧЕСКИЙ ПРОВАЛ. ЗАПРЕЩЕНО использовать `--amend`. |
| **`git push`** | КАТЕГОРИЧЕСКИ ЗАПРЕЩЕНО. Пользователь пушит сам (или командует `#push`). |
| **Команды `#...`** | Если пользователь пишет `#command` — ОБЯЗАТЕЛЬНО читай `.agent/workflows/command.md`. |

---

## ✅ ОБЯЗАТЕЛЬНЫЕ ТРЕБОВАНИЯ

### Static Analysis (PHPStan Level 9)
- **Zero Errors**: Код должен проходить `make analyze` без ошибок.
- **Strict Mixed**: Запрещено использовать `mixed` без проверок (`is_string`, `instanceof`).
- **No Unsafe Casts**: Запрещено `(string)$mixed` без гарантии скалярности.
- **Generics**: Используй `array<string, mixed>`, `iterable<User>` в PHPDoc.

### Именование
- **Классы**: `UserService`, `BookRepository`, `CreateBookCommand`
- **Методы**: `camelCase`, глагол + объект: `createBook`, `findBySlug`
- **Константы**: `UPPER_SNAKE_CASE`
- **Суффиксы**: `*Service`, `*Repository`, `*Controller`, `*Command`, `*DTO`, `*Exception`

## 🛠 ИНСТРУМЕНТЫ И СРЕДА

- **Docker-Only**: Весь код выполняется в контейнерах. Запрещено запускать `php`, `composer` или `yii` локально.
- **Make-First**: Используй ТОЛЬКО команды `make` (`make test`, `make dev`).
- **Make Help**: Не читай файл `Makefile`! Запусти `make help`, чтобы увидеть список команд.
- **Project Map**: Используй `make tree` для изучения структуры модулей и слоев.
- **LLM Navigator**: Используй `llms.txt` для поиска документации и схем данных.
- **No Local State**: Не полагайся на локальные файлы, не входящие в репозиторий.

---

## 🤖 WORKFLOW AI-АГЕНТА

### 1. Генерация и Рефакторинг (Цикл)
1. **Читай** контракт и задачу.
2. **Планируй** изменения (KISS, YAGNI).
3. **Реализуй** код (PHP 8.4, strict_types, типизация).
4. **Запускай проверки (ОБЯЗАТЕЛЬНО)**:
   - `make dev` (авто-фикс + анализ).
   - Если успех -> `make test` (все тесты).
   - Если ошибка -> Исправь и повтори.
5. **Фиксируй**: только если `make test` зеленый.
6. **Самоконтроль**: перепроверь критические запреты (особенно комментарии). Если добавил комментарии — укажи это в отчете.

### 2. Правило MD и CSS файлов
Если меняешь **только** `.md` файлы (документацию) или `.css` файлы — запускать `make dev` и `make test` **НЕ НУЖНО**.

---

## ⚙️ LINTER
**Единственная команда**: `make dev` (запускает PHPCS Fixer, Rector, PHPStan).
Используй её автоматически. Ручные `phpcs` запрещены.

---

## 🧪 ТЕСТИРОВАНИЕ
- **Тесты пишутся вместе с кодом** — не после.
- **Один тест = один сценарий**.
- **Arrange-Act-Assert** — чёткая структура.
- **Мокай внешние зависимости**.
- **Code Coverage = 100%**:
  - Считается сумма Unit + Integration.
  - Используй `@codeCoverageIgnore` для кода, который физически невозможно достичь (защита от багов PHP/Yii) или для тестирования самого фреймворка.
  - Проверка: `make cov` или `make test`.

---

## 🏛️ АРХИТЕКТУРА (Clean + Yii2)

### Слои (Deptrac Rules)
`Presentation` -> `Application` -> `Domain` <- `Infrastructure`
**Проверка**: `make deptrac` (запускать при изменении зависимостей).

### Компоненты
- **Command**: Иммутабельный объект с входными данными.
- **UseCase**: Оркестрация бизнес-логики (один публичный метод `execute`).
- **Repository**: Скрывает ActiveRecord. Доступ только через интерфейс.
- **ActiveRecord**: Только persistence (хранение). Никакой бизнес-логики.
- **Form**: Валидация входных данных (из UI/API).

### Правила
- **AR = только persistence**, без бизнес-логики.
- **Repository скрывает AR** от бизнес-логики.
- **UseCase работает через Repository**, не напрямую с AR.
- **Избегай**: бизнес-логики в AR, прямого доступа к AR в контроллере, Query building в UseCase.

---

## 🎯 SENIOR-LEVEL PRACTICES
- **Boy Scout Rule**: Оставь модуль чище, чем нашел. Увидел мелкую грязь рядом с задачей — убери.
- **SOLID, DRY, KISS, YAGNI**.
- **Dependency Inversion**: завись от интерфейсов.
- **Tell, Don't Ask**: объект сам выполняет действия.
- **Value Objects** вместо примитивов.
- **Named constructors** для ясности.

---

## 💬 ПРАВИЛА КОММЕНТАРИЕВ
- **Язык**: все обычные комментарии и `TODO` - только на русском.
- **Регистр**: после двоеточия пишем со строчной буквы, но элементы в списках - с заглавной.
- **Запрет капитанских**: не писать очевидные комментарии, которые дублируют код.
- **Минимум**: комментарии - редкий случай, только для неочевидной логики/контекста.
- **Прагматизм**: коммент нужен, если без него высок риск неверного понимания или регресса.
- **Фокус**: объяснять «почему»/ограничения/краевые случаи, а не «что делает код».
- **Формат**: 1-2 короткие строки; без воды, без «обучающих» пояснений.
- **Запрет дублирования**: не повторять имена методов/переменных и очевидный контроль потока.
- **TODO**: только с причиной и ожидаемым результатом, если это реально требуется.
- **PHPDoc**: только чтобы удовлетворить PHPStan и там, где тип не выражается сигнатурой (например, generics/shape/class-string или сложные массивы; если тип уже есть в сигнатуре - PHPDoc запрещён).
- **Разрешено**: typed arrays/generics/shape/class-string (`@param Type[]`, `@return Type[]`, `@param array<string, Foo>`, `@return array<string, mixed>`, `@return class-string`), `@throws`.
- **Code coverage**: допускаются `@codeCoverageIgnore`, `@codeCoverageIgnoreStart/End` только при реальной необходимости.
- **Запрещено**: описывать очевидные типы, поведение метода или дублировать сигнатуру.

## 🔄 ЖИВОЙ ДОКУМЕНТ
- **Правило отклонений**: Если контракт требует технически опасного действия (security/data loss) — **СТОП**. Сообщи об этом.
- **Правило обучения**: Если ты ошибся и пользователь тебя поправил — **ПРЕДЛОЖИ** обновление контракта, чтобы ошибка не повторилась.