# AI-First Workflow

[← Назад в README](../README.md)

Проект разрабатывается с применением методологии **AI-First Development**. Это означает, что AI-агенты (Gemini, Claude) являются полноценными участниками команды, следуют строгому контракту и используют автоматизированные рабочие процессы (workflows).

## 🤖 AI Contract (Конституция)

В корне проекта находится файл `CLAUDE.md` (симлинки: `GEMINI.md`, `.cursorrules`), который является **единым источником истины** для AI.
*   **Правила:** архитектурные ограничения, стиль кода, запретные паттерны.
*   **Команды:** инструкции по сборке, тестированию и деплою.
*   **Приоритеты:** безопасность данных, атомарность коммитов, чистота архитектуры.

> Любое взаимодействие с AI начинается с загрузки этого контекста.

## ⚡ Workflows (Рабочие процессы)

Для стандартизации рутинных задач используются сценарии в `.agent/workflows/`. Вызов осуществляется через команды в чате или автоматически.

### Основные сценарии
*   `#/commit` — атомарный коммит изменений. Анализирует `git status`, запускает `make dev` и формирует сообщение по конвенции Conventional Commits.
*   `#/changelog` — генерация `CHANGELOG.md`. Собирает новые коммиты, переводит их, группирует (Feature, Fix, Refactor) и обновляет версию.
*   `#/readme` — актуализация документации. Обновляет метрики тестов, стек технологий и описание архитектуры в `README.md`.

## 🛠 Инструментарий

### Генерация документации
*   `make docs` — строит API документацию (классы, методы) на основе PHPDoc.
*   `make swagger` — генерирует OpenAPI спецификацию (`swagger.json`) для REST API.
*   `make repomix` — собирает весь код проекта в один Markdown-файл (`repomix-output.md`) для подачи в контекст LLM.

### Системные команды
*   `make ai` — настраивает окружение для AI (создает симлинки контрактов, папки агентов).
*   `make shell` — запускает интерактивную консоль (`PsySH`) для глубокого анализа кода агентом.

## 🧩 Принципы взаимодействия

1.  **Zero Hallucination:** Агент не придумывает команды, а использует `Makefile`.
2.  **No Proactive Commits:** Агент никогда не делает `git commit` сам без явной команды или подтверждения.
3.  **Self-Correction:** При ошибке тестов (`make dev` failed) агент обязан исправить код до зеленого статуса.
