# AI-First Workflow

[← Назад в README](../README.md)

Проект разрабатывается с применением методологии **AI-First Development**. AI-агенты (Gemini, Claude, Cursor) — полноценные участники команды, следуют строгому контракту и автоматизированным workflow. Эта страница объясняет, как устроена работа с AI в проекте и как её использовать.

---

## 🧠 Иерархия контекста

AI-агенты загружают контекст по цепочке:

```
CLAUDE.md (bootloader)
    ↓
docs/ai/contract.md (конституция)
    ↓
.agent/workflows/*.md (сценарии)
    ↓
.codex/skills/*.md (навыки)
```

| Уровень | Назначение | Пример |
|---------|------------|--------|
| 🚀 Bootloader | Точка входа для любого AI-агента | `CLAUDE.md`, `GEMINI.md`, `.cursorrules` |
| 📜 Конституция | Все запреты, правила, приоритеты | `docs/ai/contract.md` |
| ⚡ Workflows | Пошаговые сценарии для рутинных задач | `#commit`, `#changelog`, `#readme` |
| 🧩 Skills | Детальные инструкции для Codex/Devin | `.codex/skills/readme/SKILL.md` |

> ⚠️ **Правило загрузки:** любое взаимодействие с AI начинается с чтения `CLAUDE.md` → `docs/ai/contract.md`. Если контракт недоступен — работа прекращается.

---

## 📜 Конституция

Файл `docs/ai/contract.md` — **единственный источник истины** для AI.

### Что содержит

- 🚫 **Критические запреты:** Service Locator, FQCN, коммиты без запроса, английские ответы.
- ✅ **Обязательные требования:** PHPStan Level 9, strict_types, 100% coverage.
- 🏛️ **Архитектура:** слои (presentation → application → domain ← infrastructure), правила Deptrac.
- 🛠️ **Инструменты:** Docker-only, make-first, проверки через `make dev`.
- 💬 **Комментарии:** только на русском, минимализм, PHPDoc только для PHPStan.

### Живой документ

Если агент ошибся и разработчик его поправил — агент **предлагает** обновление контракта, чтобы ошибка не повторилась.

---

## ⚡ Workflows

Стандартизированные сценарии хранятся в `.agent/workflows/`. Вызов — через команду `#название` в чате.

| Команда | Назначение |
|---------|------------|
| `#commit` | Атомарный коммит: `git status` → `make dev` → сообщение по Conventional Commits |
| `#changelog` | Генерация CHANGELOG.md: сбор коммитов → перевод → группировка (feature, fix, refactor) |
| `#readme` | Актуализация документации: метрики `make test`, стек, архитектурные формулировки |
| `#go` | Загрузка контекста (bootloader): чтение контракта и подготовка к работе |

> 💡 **Порядок чтения:** сначала читай `#go`, затем вызывай нужный workflow.

---

## 🛠️ Инструменты

### Проверки качества

| Команда | Назначение |
|---------|------------|
| `make dev` | Авто-фикс (PHPCS Fixer, Rector) + статический анализ (PHPStan Level 9) |
| `make test` | Полный прогон тестов (unit + integration) с 100% coverage |
| `make test-e2e` | E2E тесты в браузере |
| `make deptrac` | Проверка архитектурных зависимостей |

### Документация

| Команда | Назначение |
|---------|------------|
| `make tree` | Структура проекта (слои, модули) |
| `make repomix` | Весь код в один Markdown для контекста LLM |
| `bin/validate-docs` | Проверка метрик и ссылок в README.md |

### Среда разработки

| Команда | Назначение |
|---------|------------|
| `make shell` | Интерактивная консоль PsySH для анализа кода |
| `make ai` | Настройка окружения (симлинки контрактов, папки агентов) |

---

## 🎯 Принципы взаимодействия

### Загрузка контекста

1. 🚀 **Bootloader:** прочитай `CLAUDE.md` (или `GEMINI.md`, `.cursorrules`).
2. 📜 **Конституция:** загрузи `docs/ai/contract.md` целиком.
3. ⚡ **Workflow:** если вызван `#command` — прочитай `.agent/workflows/command.md`.

### Безопасность

| Правило | Описание |
|---------|----------|
| 🔒 No proactive commits | `git commit` запрещен без явного запроса `#commit` |
| 🔒 No push | `git push` выполняет только разработчик |
| 🐳 Docker-only | Запрещено запускать `php`, `composer`, `yii` локально |
| 🔧 Make-first | Команды — через `make`. Список — `make help` |

### Качество

| Правило | Описание |
|---------|----------|
| 🔄 Self-correction | При ошибке `make dev` — исправить код до зелёного статуса |
| 📐 Архитектурная сверка | Формулировки в README/ARCHITECTURE сверять с кодом |
| 📝 Единая терминология | Переводить: контроллеры, формы, представления, слои, очереди, фильтры. Не переводить: Value Object, Use Case, DTO, Repository, Event, Mapper, Handler |

### Метрики

| Правило | Описание |
|---------|----------|
| 🧪 Тесты | Обновлять по `make test` или данным разработчика |
| 🦠 Mutation testing | `make infection` не запускать без явного запроса |
| ✅ Валидация | Перед коммитом документации — `bin/validate-docs` |

---

## 🧩 Best practices

### Эффективный контекст

- 📁 **Иерархия файлов:** bootloader → контракт → workflow → код.
- 🎯 **Минимум шума:** не загружать весь проект, использовать `make tree` и `make repomix` для навигации.
- 🔬 **Фокус:** один workflow — одна задача.

### Предсказуемость

- ⚙️ **Детерминизм:** одни и те же входные данные → одинаковый результат.
- 🧱 **Атомарность:** один коммит — одна логическая единица изменений.
- ✅ **Проверяемость:** каждый шаг завершается `make dev` / `make test`.

### Обучение

- 🔄 **Feedback loop:** ошибки агента → обновление контракта.
- 🧩 **Skills:** сложные сценарии выносить в `.codex/skills/` для переиспользования.
- 📋 **Changelog:** история изменений контракта документируется.

---

## 🚀 Как начать работу с AI

1. Убедись, что проект склонирован и `make install` выполнен.
2. Запусти `make ai` — это создаст симлинки для AI-агентов.
3. Открой проект в IDE с AI-ассистентом (Cursor, Gemini Code Assist, Claude).
4. AI автоматически прочитает `CLAUDE.md` и загрузит контракт.
5. Используй команды `#commit`, `#changelog`, `#readme` для рутинных задач.

> 💡 **Совет:** если AI ведёт себя странно — попроси его перечитать контракт командой `#go`.
